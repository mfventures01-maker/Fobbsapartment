export const SHIFT_STATUS = {
    REQUESTED: "requested",
    OPEN: "open",
    PENDING_DECLARATION: "pending_declaration",
    AWAITING_APPROVAL: "awaiting_approval",
    CLOSED: "closed"
} as const;
